#include "loginwidget.h"

#include <QVBoxLayout>
#include <qlabel.h>

LoginWidget::LoginWidget(MainWindow *window, QWidget *parent)
    :ContentWidget(window, parent) {

    QLabel *label = new QLabel("Login Widget");
    QVBoxLayout *layout = new QVBoxLayout;
    layout->addWidget(label);
    setLayout(layout);
}

LoginWidget::~LoginWidget() {
}
